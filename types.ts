type GameEntity = { id: string; name: string; position: { x: number; y: number }; health: number; }; type Player = GameEntity & { score: number; level: number; inventory: string[]; }; type Enemy = GameEntity & { damage: number; loot: string[]; }; type GameConfig = { maxPlayers: number; gameDuration: number; spawnRate: number; }; type GameState = { players: Player[]; enemies: Enemy[]; timeElapsed: number; isRunning: boolean; }; function createPlayer(name: string): Player { return { id: crypto.randomUUID(), name, position: { x: 0, y: 0 }, health: 100, score: 0, level: 1, inventory: [] }; } function createEnemy(name: string, damage: number): Enemy { return { id: crypto.randomUUID(), name, position: { x: Math.random() * 100, y: Math.random() * 100 }, health: 100, damage, loot: [] }; }