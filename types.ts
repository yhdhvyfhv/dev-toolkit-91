type GameEntity = {\n    id: string;\n    position: { x: number; y: number; };\n    velocity: { x: number; y: number; };\n    update: () => void;\n};\n\ntype GameState = {\n    entities: GameEntity[];\n    time: number;\n    addEntity: (entity: GameEntity) => void;\n    removeEntity: (id: string) => void;\n    updateEntities: () => void;\n};\n\nconst createGameEntity = (id: string, x: number, y: number): GameEntity => ({\n    id,\n    position: { x, y },\n    velocity: { x: 0, y: 0 },\n    update() {\n        this.position.x += this.velocity.x;\n        this.position.y += this.velocity.y;\n    }\n});\n\nconst createGameState = (): GameState => ({\n    entities: [],\n    time: 0,\n    addEntity(entity) { this.entities.push(entity); },\n    removeEntity(id) { this.entities = this.entities.filter(e => e.id !== id); },\n    updateEntities() { this.entities.forEach(entity => entity.update()); }\n});\n